     1→     1→-- ============================================================================
     2→     2→-- RestoPanel · Migración 0018 — Correcciones críticas de auditoría
     3→     3→-- ============================================================================
     4→     4→-- Esta migración parchea TODOS los bugs detectados en la auditoría:
     5→     5→--
     6→     6→--   1. Recursión RLS en policies inline de 0003 (usaban
     7→     7→--      `exists (select 1 from users u where u.id = auth.uid() ...)`
     8→     8→--      en lugar de is_current_user_super_admin()).
     9→     9→--   2. transfer_reservation() no validaba organización ni tabla origen.
    10→    10→--   3. order_items.menu_item_id era ON DELETE CASCADE (borraba historial).
    11→    11→--   4. update_customer_metrics() no decrementaba al revertir estado.
    12→    12→--   5. tables.group_id no tenía FK (table_groups no existía).
    13→    13→--   6. Falta UNIQUE en subscription_history para idempotencia webhook.
    14→    14→--   7. Faltan índices en FKs críticas.
    15→    15→--   8. Faltan triggers touch_updated_at() en 11 tablas.
    16→    16→--   9. Faltan UNIQUE en customers(organization_id, phone/email).
    17→    17→--  10. Faltan CHECK constraints en columnas tipo enum.
    18→    18→--  11. Faltan policies DELETE en varias tablas.
    19→    19→--  12. subscription_plans: añadir UNIQUE en stripe_price_id_monthly/yearly.
    20→    20→--
    21→    21→-- Es idempotente: se puede ejecutar varias veces sin error.
    22→    22→-- ============================================================================
    23→    23→
    24→    24→-- ============================================================
    25→    25→-- 1. ELIMINAR Y RECREAR POLICIES RECURSIVAS DE 0003
    26→    26→-- ============================================================
    27→    27→-- Las policies originales usaban `exists (select 1 from users u where u.id = auth.uid() and u.is_super_admin = true)`
    28→    28→-- que causa recursión infinita porque la policy sobre `users` también llama a la policy sobre `users`.
    29→    29→-- Las reescribimos usando is_current_user_super_admin() que lee del JWT (sin tocar public.users).
    30→    30→
    31→    31→-- audit_logs
    32→    32→DROP POLICY IF EXISTS audit_logs_super_admin_select ON audit_logs;
    33→    33→DROP POLICY IF EXISTS audit_logs_super_admin_insert ON audit_logs;
    34→    34→DROP POLICY IF EXISTS audit_logs_super_admin_update ON audit_logs;
    35→    35→DROP POLICY IF EXISTS audit_logs_super_admin_delete ON audit_logs;
    36→    36→CREATE POLICY audit_logs_super_admin_select ON audit_logs
    37→    37→  FOR SELECT USING (is_current_user_super_admin());
    38→    38→CREATE POLICY audit_logs_super_admin_insert ON audit_logs
    39→    39→  FOR INSERT WITH CHECK (is_current_user_super_admin());
    40→    40→CREATE POLICY audit_logs_super_admin_update ON audit_logs
    41→    41→  FOR UPDATE USING (is_current_user_super_admin()) WITH CHECK (is_current_user_super_admin());
    42→    42→CREATE POLICY audit_logs_super_admin_delete ON audit_logs
    43→    43→  FOR DELETE USING (is_current_user_super_admin());
    44→    44→
    45→    45→-- users — super_admin policies (no recursivas)
    46→    46→DROP POLICY IF EXISTS users_super_admin_select ON users;
    47→    47→DROP POLICY IF EXISTS users_super_admin_insert ON users;
    48→    48→DROP POLICY IF EXISTS users_super_admin_update ON users;
    49→    49→DROP POLICY IF EXISTS users_super_admin_delete ON users;
    50→    50→CREATE POLICY users_super_admin_select ON users
    51→    51→  FOR SELECT USING (is_current_user_super_admin());
    52→    52→CREATE POLICY users_super_admin_insert ON users
    53→    53→  FOR INSERT WITH CHECK (is_current_user_super_admin());
    54→    54→CREATE POLICY users_super_admin_update ON users
    55→    55→  FOR UPDATE USING (is_current_user_super_admin()) WITH CHECK (is_current_user_super_admin());
    56→    56→CREATE POLICY users_super_admin_delete ON users
    57→    57→  FOR DELETE USING (is_current_user_super_admin());
    58→    58→
    59→    59→-- organizations — super_admin policies
    60→    60→DROP POLICY IF EXISTS organizations_super_admin_all ON organizations;
    61→    61→CREATE POLICY organizations_super_admin_all ON organizations
    62→    62→  FOR ALL USING (is_current_user_super_admin()) WITH CHECK (is_current_user_super_admin());
    63→    63→
    64→    64→-- Dynamic loop: replace any policy that still uses the recursive
    65→    65→-- `exists (select 1 from users ...)` pattern with the helper function.
    66→    66→DO $$
    67→    67→DECLARE
    68→    68→  t text;
    69→    69→  policies_record record;
    70→    70→BEGIN
    71→    71→  FOR t IN SELECT unnest(ARRAY[
    72→    72→    'categories','menu_items','tables','orders','order_items',
    73→    73→    'reservations','organization_settings','verification_tokens'
    74→    74→  ])
    75→    75→  LOOP
    76→    76→    FOR policies_record IN
    77→    77→      SELECT policyname FROM pg_policies
    78→    78→      WHERE schemaname = 'public' AND tablename = t
    79→    79→        AND qual LIKE '%exists (select 1 from users u where u.id = auth.uid() and u.is_super_admin = true)%'
    80→    80→    LOOP
    81→    81→      EXECUTE format('DROP POLICY IF EXISTS %I ON %I;', policies_record.policyname, t);
    82→    82→      EXECUTE format(
    83→    83→        'CREATE POLICY %I ON %I FOR ALL USING (organization_id = current_user_org_id() OR is_current_user_super_admin()) WITH CHECK (organization_id = current_user_org_id() OR is_current_user_super_admin());',
    84→    84→        replace(policies_record.policyname, '_select', '_all'),
    85→    85→        t
    86→    86→      );
    87→    87→    END LOOP;
    88→    88→  END LOOP;
    89→    89→END $$;
    90→    90→
    91→    91→-- ============================================================
    92→    92→-- 2. REESCRIBIR transfer_reservation() CON VALIDACIÓN DE ORG
    93→    93→-- ============================================================
    94→    94→-- Antes: cualquier usuario podía mover una reserva de cualquier tenant.
    95→    95→-- Ahora: valida que la reserva, mesa origen y mesa destino pertenezcan
    96→    96→-- a la organización del JWT actual.
    97→    97→--
    98→    98→-- NOTA: PostgreSQL NO permite cambiar nombres de parámetros con
    99→    99→-- CREATE OR REPLACE FUNCTION. Hay que hacer DROP primero.
   100→   100→-- Usamos la signatura completa (uuid, uuid, uuid) para no afectar
   101→   101→-- a otras posibles versiones de la función.
   102→   102→
   103→   103→DROP FUNCTION IF EXISTS transfer_reservation(uuid, uuid, uuid);
   104→   104→DROP FUNCTION IF EXISTS transfer_reservation(uuid, uuid);
   105→   105→
   106→   106→CREATE OR REPLACE FUNCTION transfer_reservation(
   107→   107→  p_reservation_id uuid,
   108→   108→  p_new_table_id uuid,
   109→   109→  p_old_table_id uuid DEFAULT NULL
   110→   110→)
   111→   111→RETURNS boolean
   112→   112→LANGUAGE plpgsql
   113→   113→SECURITY DEFINER
   114→   114→SET search_path = public, pg_temp
   115→   115→AS $$
   116→   116→DECLARE
   117→   117→  v_reservation record;
   118→   118→  v_new_table record;
   119→   119→  v_org_id uuid;
   120→   120→BEGIN
   121→   121→  -- Get the caller's organization from the JWT claim.
   122→   122→  v_org_id := current_user_org_id();
   123→   123→  IF v_org_id IS NULL AND NOT is_current_user_super_admin() THEN
   124→   124→    RAISE EXCEPTION 'No organization context';
   125→   125→  END IF;
   126→   126→
   127→   127→  -- Load the reservation (must exist).
   128→   128→  SELECT * INTO v_reservation
   129→   129→  FROM reservations
   130→   130→  WHERE id = p_reservation_id
   131→   131→  FOR UPDATE;
   132→   132→
   133→   133→  IF NOT FOUND THEN
   134→   134→    RAISE EXCEPTION 'Reservation not found';
   135→   135→  END IF;
   136→   136→
   137→   137→  -- Org isolation: the reservation must belong to the caller's org
   138→   138→  -- (unless super admin).
   139→   139→  IF v_org_id IS NOT NULL AND v_reservation.organization_id != v_org_id THEN
   140→   140→    RAISE EXCEPTION 'Forbidden: reservation does not belong to your organization';
   141→   141→  END IF;
   142→   142→
   143→   143→  -- If p_old_table_id was provided, it must match the reservation's
   144→   144→  -- current table_id (prevents race conditions where two clients
   145→   145→  -- transfer the same reservation simultaneously).
   146→   146→  IF p_old_table_id IS NOT NULL AND v_reservation.table_id IS NOT NULL
   147→   147→     AND p_old_table_id::text != v_reservation.table_id::text THEN
   148→   148→    RAISE EXCEPTION 'Old table id does not match reservation''s current table';
   149→   149→  END IF;
   150→   150→
   151→   151→  -- Load the new table (must exist and belong to the same org).
   152→   152→  SELECT * INTO v_new_table
   153→   153→  FROM tables
   154→   154→  WHERE id = p_new_table_id
   155→   155→  FOR UPDATE;
   156→   156→
   157→   157→  IF NOT FOUND THEN
   158→   158→    RAISE EXCEPTION 'Target table not found';
   159→   159→  END IF;
   160→   160→
   161→   161→  IF v_org_id IS NOT NULL AND v_new_table.organization_id != v_org_id THEN
   162→   162→    RAISE EXCEPTION 'Forbidden: target table does not belong to your organization';
   163→   163→  END IF;
   164→   164→
   165→   165→  -- All three operations in a single transaction (atomic):
   166→   166→  -- 1. Update reservation to point to new table.
   167→   167→  -- 2. Free the old table (mark AVAILABLE) — only if it had this reservation.
   168→   168→  -- 3. Mark the new table as RESERVED.
   169→   169→  UPDATE reservations
   170→   170→  SET table_id = p_new_table_id, updated_at = now()
   171→   171→  WHERE id = p_reservation_id;
   172→   172→
   173→   173→  IF v_reservation.table_id IS NOT NULL THEN
   174→   174→    UPDATE tables
   175→   175→    SET status = 'AVAILABLE', updated_at = now()
   176→   176→    WHERE id = v_reservation.table_id
   177→   177→      AND organization_id = v_reservation.organization_id;
   178→   178→  END IF;
   179→   179→
   180→   180→  UPDATE tables
   181→   181→  SET status = 'RESERVED', updated_at = now()
   182→   182→  WHERE id = p_new_table_id
   183→   183→    AND organization_id = v_reservation.organization_id;
   184→   184→
   185→   185→  RETURN true;
   186→   186→END;
   187→   187→$$;
   188→   188→
   189→   189→COMMENT ON FUNCTION transfer_reservation IS 'Atomic transfer of a reservation between tables. Validates org ownership. All 3 operations (update reservation, free old table, reserve new table) happen in a single transaction with automatic rollback on failure.';
   190→   190→
   191→   191→-- ============================================================
   192→   192→-- 3. order_items.menu_item_id: ON DELETE SET NULL (no CASCADE)
   193→   193→-- ============================================================
   194→   194→-- Borra el FK viejo y crea uno nuevo con SET NULL para que borrar
   195→   195→-- un plato NO destruya el historial de pedidos.
   196→   196→
   197→   197→ALTER TABLE order_items
   198→   198→  DROP CONSTRAINT IF EXISTS order_items_menu_item_id_fkey;
   199→   199→
   200→   200→ALTER TABLE order_items
   201→   201→  ADD CONSTRAINT order_items_menu_item_id_fkey
   202→   202→  FOREIGN KEY (menu_item_id) REFERENCES menu_items(id)
   203→   203→  ON DELETE SET NULL;
   204→   204→
   205→   205→-- ============================================================
   206→   206→-- 4. update_customer_metrics() con decremento al revertir
   207→   207→-- ============================================================
   208→   208→-- Versión corregida: si una reserva pasa de COMPLETED a CANCELLED,
   209→   209→-- resta 1 del contador visits_count (en lugar de solo sumar).
   210→   210→--
   211→   211→-- NOTA: Mismo problema que con transfer_reservation — DROP IF EXISTS
   212→   212→-- primero para evitar el error 42P13 si la función existía con otra
   213→   213→-- signatura. CASCADE por si había triggers asociados.
   214→   214→
   215→   215→DROP FUNCTION IF EXISTS update_customer_metrics() CASCADE;
   216→   216→
   217→   217→CREATE OR REPLACE FUNCTION update_customer_metrics()
   218→   218→RETURNS trigger
   219→   219→LANGUAGE plpgsql
   220→   220→SECURITY DEFINER
   221→   221→SET search_path = public, pg_temp
   222→   222→AS $$
   223→   223→DECLARE
   224→   224→  v_old_status text;
   225→   225→  v_new_status text;
   226→   226→  v_customer_id uuid;
   227→   227→BEGIN
   228→   228→  v_old_status := COALESCE(OLD.status, '');
   229→   229→  v_new_status := COALESCE(NEW.status, '');
   230→   230→
   231→   231→  -- Only proceed if status changed.
   232→   232→  IF v_old_status = v_new_status THEN
   233→   233→    RETURN NEW;
   234→   234→  END IF;
   235→   235→
   236→   236→  -- Find the customer (reservations store customer_id if linked).
   237→   237→  v_customer_id := NEW.customer_id;
   238→   238→  IF v_customer_id IS NULL THEN
   239→   239→    RETURN NEW;
   240→   240→  END IF;
   241→   241→
   242→   242→  -- Increment when entering COMPLETED.
   243→   243→  IF v_new_status = 'COMPLETED' AND v_old_status != 'COMPLETED' THEN
   244→   244→    UPDATE customers
   245→   245→    SET visits_count = COALESCE(visits_count, 0) + 1,
   246→   246→        last_visit_at = now(),
   247→   247→        updated_at = now()
   248→   248→    WHERE id = v_customer_id;
   249→   249→  END IF;
   250→   250→
   251→   251→  -- Decrement when leaving COMPLETED (e.g., reversal/correction).
   252→   252→  IF v_old_status = 'COMPLETED' AND v_new_status != 'COMPLETED' THEN
   253→   253→    UPDATE customers
   254→   254→    SET visits_count = GREATEST(0, COALESCE(visits_count, 1) - 1),
   255→   255→        updated_at = now()
   256→   256→    WHERE id = v_customer_id;
   257→   257→  END IF;
   258→   258→
   259→   259→  RETURN NEW;
   260→   260→END;
   261→   261→$$;
   262→   262→
   263→   263→-- Recrear los 2 triggers que el DROP FUNCTION ... CASCADE borró
   264→   264→-- (los había creado 0006_crm_customers.sql).
   265→   265→DROP TRIGGER IF EXISTS reservations_update_customer_metrics ON reservations;
   266→   266→CREATE TRIGGER reservations_update_customer_metrics
   267→   267→  AFTER UPDATE ON reservations
   268→   268→  FOR EACH ROW EXECUTE FUNCTION update_customer_metrics();
   269→   269→
   270→   270→DROP TRIGGER IF EXISTS reservations_insert_customer_metrics ON reservations;
   271→   271→CREATE TRIGGER reservations_insert_customer_metrics
   272→   272→  AFTER INSERT ON reservations
   273→   273→  FOR EACH ROW EXECUTE FUNCTION update_customer_metrics();
   274→   274→
   275→   275→-- ============================================================
   276→   276→-- 5. CREAR TABLA table_groups (FALTABA) + FK EN tables.group_id
   277→   277→-- ============================================================
   278→   278→CREATE TABLE IF NOT EXISTS table_groups (
   279→   279→  id              uuid primary key default gen_random_uuid(),
   280→   280→  name            text not null,
   281→   281→  color           text not null default '#C5A059',
   282→   282→  organization_id uuid not null references organizations(id) on delete cascade,
   283→   283→  created_at      timestamptz not null default now(),
   284→   284→  updated_at      timestamptz not null default now(),
   285→   285→  unique (organization_id, name)
   286→   286→);
   287→   287→
   288→   288→CREATE INDEX IF NOT EXISTS table_groups_org_idx ON table_groups(organization_id);
   289→   289→
   290→   290→ALTER TABLE table_groups ENABLE ROW LEVEL SECURITY;
   291→   291→DROP POLICY IF EXISTS table_groups_tenant_select ON table_groups;
   292→   292→CREATE POLICY table_groups_tenant_select ON table_groups
   293→   293→  FOR SELECT USING (organization_id = current_user_org_id() or is_current_user_super_admin());
   294→   294→DROP POLICY IF EXISTS table_groups_tenant_insert ON table_groups;
   295→   295→CREATE POLICY table_groups_tenant_insert ON table_groups
   296→   296→  FOR INSERT WITH CHECK (organization_id = current_user_org_id());
   297→   297→DROP POLICY IF EXISTS table_groups_tenant_update ON table_groups;
   298→   298→CREATE POLICY table_groups_tenant_update ON table_groups
   299→   299→  FOR UPDATE USING (organization_id = current_user_org_id());
   300→   300→DROP POLICY IF EXISTS table_groups_tenant_delete ON table_groups;
   301→   301→CREATE POLICY table_groups_tenant_delete ON table_groups
   302→   302→  FOR DELETE USING (organization_id = current_user_org_id());
   303→   303→
   304→   304→-- Añadir FK a tables.group_id (antes era una columna suelta)
   305→   305→-- Primero eliminamos cualquier dato huérfano
   306→   306→UPDATE tables SET group_id = NULL
   307→   307→WHERE group_id IS NOT NULL
   308→   308→  AND group_id NOT IN (SELECT id FROM table_groups);
   309→   309→
   310→   310→ALTER TABLE tables
   311→   311→  DROP CONSTRAINT IF EXISTS tables_group_id_fkey;
   312→   312→
   313→   313→ALTER TABLE tables
   314→   314→  ADD CONSTRAINT tables_group_id_fkey
   315→   315→  FOREIGN KEY (group_id) REFERENCES table_groups(id)
   316→   316→  ON DELETE SET NULL;
   317→   317→
   318→   318→-- ============================================================
   319→   319→-- 6. UNIQUE EN subscription_history PARA IDEMPOTENCIA WEBHOOK
   320→   320→-- ============================================================
   321→   321→-- Stripe reenvía eventos cuando recibe 500. Necesitamos un UNIQUE
   322→   322→-- compuesto para que el ON CONFLICT del webhook funcione.
   323→   323→-- Antes de crear el índice, eliminamos duplicados existentes.
   324→   324→-- Envuelto en DO $$ para no fallar si subscription_history no existe.
   325→   325→
   326→   326→DO $$
   327→   327→BEGIN
   328→   328→  IF EXISTS (SELECT 1 FROM information_schema.tables
   329→   329→             WHERE table_schema = 'public' AND table_name = 'subscription_history') THEN
   330→   330→
   331→   331→    -- 6a. De-duplicar subscription_history por (organization_id, event_type, details)
   332→   332→    -- Mantener solo la fila más reciente (la de id mayor — usamos UUID,
   333→   333→    -- así que comparamos por created_at).
   334→   334→    DELETE FROM subscription_history a
   335→   335→    USING subscription_history b
   336→   336→    WHERE a.organization_id IS NOT DISTINCT FROM b.organization_id
   337→   337→      AND a.event_type = b.event_type
   338→   338→      AND a.details::text IS NOT DISTINCT FROM b.details::text
   339→   339→      AND a.created_at < b.created_at;
   340→   340→
   341→   341→    -- 6b. Crear el índice UNIQUE
   342→   342→    CREATE UNIQUE INDEX IF NOT EXISTS subscription_history_org_event_details_uniq
   343→   343→      ON subscription_history(organization_id, event_type, details);
   344→   344→  END IF;
   345→   345→END $$;
   346→   346→
   347→   347→-- UNIQUE en invoices.stripe_invoice_id (ya tiene UNIQUE en la tabla)
   348→   348→-- UNIQUE en payment_methods.stripe_payment_method_id (ya tiene UNIQUE en la tabla)
   349→   349→
   350→   350→-- UNIQUE en subscription_plans.stripe_price_id_monthly/yearly
   351→   351→-- (para que el webhook pueda resolver plan_id por price_id sin ambigüedad)
   352→   352→CREATE UNIQUE INDEX IF NOT EXISTS subscription_plans_stripe_price_monthly_uniq
   353→   353→  ON subscription_plans(stripe_price_id_monthly)
   354→   354→  WHERE stripe_price_id_monthly IS NOT NULL;
   355→   355→
   356→   356→CREATE UNIQUE INDEX IF NOT EXISTS subscription_plans_stripe_price_yearly_uniq
   357→   357→  ON subscription_plans(stripe_price_id_yearly)
   358→   358→  WHERE stripe_price_id_yearly IS NOT NULL;
   359→   359→
   360→   360→-- ============================================================
   361→   361→-- 7. ÍNDICES FALTANTES EN FKs CRÍTICAS
   362→   362→-- ============================================================
   363→   363→-- Cada CREATE INDEX se envuelve en un DO $$ que verifica si la
   364→   364→-- tabla existe antes de intentar crear el índice. Así no falla
   365→   365→-- si alguna migración intermedia no se ejecutó.
   366→   366→
   367→   367→DO $$
   368→   368→DECLARE
   369→   369→  v_table text;
   370→   370→  v_index text;
   371→   371→  v_col text;
   372→   372→BEGIN
   373→   373→  -- (tabla, índice, columna)
   374→   374→  FOR v_table, v_index, v_col IN SELECT * FROM (VALUES
   375→   375→    ('order_items',         'order_items_menu_item_id_idx',     'menu_item_id'),
   376→   376→    ('orders',              'orders_table_id_idx',              'table_id'),
   377→   377→    ('reservations',        'reservations_table_id_idx',        'table_id'),
   378→   378→    ('chat_messages',       'chat_messages_user_id_idx',        'user_id'),
   379→   379→    ('role_permissions',    'role_permissions_permission_id_idx','permission_id'),
   380→   380→    ('user_roles',          'user_roles_role_id_idx',           'role_id'),
   381→   381→    ('order_items',         'order_items_order_id_idx',         'order_id'),
   382→   382→    ('reservations',        'reservations_customer_id_idx',     'customer_id'),
   383→   383→    ('menu_items',          'menu_items_category_id_idx',       'category_id'),
   384→   384→    ('reservations',        'reservations_status_idx',          'status'),
   385→   385→    ('reservations',        'reservations_date_idx',            'date'),
   386→   386→    ('orders',              'orders_status_idx',                'status')
   387→   387→  ) AS t(tab, idx, col)
   388→   388→  LOOP
   389→   389→    IF EXISTS (SELECT 1 FROM information_schema.columns
   390→   390→               WHERE table_schema = 'public' AND table_name = v_table
   391→   391→                 AND column_name = v_col) THEN
   392→   392→      EXECUTE format('CREATE INDEX IF NOT EXISTS %I ON %I(%I);', v_index, v_table, v_col);
   393→   393→    END IF;
   394→   394→  END LOOP;
   395→   395→
   396→   396→  -- orders_created_at_idx usa ORDER BY (desc) que requiere sintaxis especial
   397→   397→  IF EXISTS (SELECT 1 FROM information_schema.columns
   398→   398→             WHERE table_schema = 'public' AND table_name = 'orders'
   399→   399→               AND column_name = 'created_at') THEN
   400→   400→    CREATE INDEX IF NOT EXISTS orders_created_at_idx ON orders(created_at DESC);
   401→   401→  END IF;
   402→   402→END $$;
   403→   403→
   404→   404→-- ============================================================
   405→   405→-- 8. TRIGGERS touch_updated_at() FALTANTES
   406→   406→-- ============================================================
   407→   407→DO $$
   408→   408→DECLARE
   409→   409→  t text;
   410→   410→BEGIN
   411→   411→  FOR t IN SELECT unnest(ARRAY[
   412→   412→    'zones','customers','customer_tags','staff_shifts','chat_channels',
   413→   413→    'subscription_plans','feature_flag_overrides','system_settings',
   414→   414→    'email_queue','organization_usage','invoices','table_groups'
   415→   415→  ])
   416→   416→  LOOP
   417→   417→    IF EXISTS (SELECT 1 FROM information_schema.columns
   418→   418→               WHERE table_schema = 'public' AND table_name = t
   419→   419→                 AND column_name = 'updated_at') THEN
   420→   420→      EXECUTE format('DROP TRIGGER IF EXISTS %I_touch ON %I;', t, t);
   421→   421→      EXECUTE format(
   422→   422→        'CREATE TRIGGER %I_touch BEFORE UPDATE ON %I FOR EACH ROW EXECUTE FUNCTION touch_updated_at();',
   423→   423→        t, t
   424→   424→      );
   425→   425→    END IF;
   426→   426→  END LOOP;
   427→   427→END $$;
   428→   428→
   429→   429→-- ============================================================
   430→   430→-- 9. UNIQUE EN customers(organization_id, phone) y (organization_id, email)
   431→   431→-- ============================================================
   432→   432→-- IMPORTANTE: Si la DB ya contiene duplicados (mismo phone o email
   433→   433→-- repetido en la misma organización), el CREATE UNIQUE INDEX fallará
   434→   434→-- con error 23505. Por eso primero de-duplicamos manteniendo solo
   435→   435→-- la fila más reciente (MAX(created_at)) y reasignando cualquier
   436→   436→-- referencia externa a la fila superviviente.
   437→   437→
   438→   438→-- 9a. De-duplicar por (organization_id, phone)
   439→   439→-- Mantener la fila con el created_at más reciente; borrar el resto.
   440→   440→-- Antes de borrar, mover cualquier reservation/order que apunte a
   441→   441→-- los duplicados para que apunte al superviviente.
   442→   442→DO $$
   443→   443→DECLARE
   444→   444→  dup_row record;
   445→   445→  survivor_id uuid;
   446→   446→BEGIN
   447→   447→  FOR dup_row IN
   448→   448→    SELECT organization_id, phone
   449→   449→    FROM customers
   450→   450→    WHERE phone IS NOT NULL
   451→   451→    GROUP BY organization_id, phone
   452→   452→    HAVING COUNT(*) > 1
   453→   453→  LOOP
   454→   454→    -- Elegir el superviviente: el de created_at más reciente
   455→   455→    SELECT id INTO survivor_id
   456→   456→    FROM customers
   457→   457→    WHERE organization_id = dup_row.organization_id
   458→   458→      AND phone = dup_row.phone
   459→   459→    ORDER BY created_at DESC, updated_at DESC
   460→   460→    LIMIT 1;
   461→   461→
   462→   462→    -- Mover reservations que apuntaban a duplicados
   463→   463→    UPDATE reservations
   464→   464→    SET customer_id = survivor_id
   465→   465→    WHERE customer_id IN (
   466→   466→      SELECT id FROM customers
   467→   467→      WHERE organization_id = dup_row.organization_id
   468→   468→        AND phone = dup_row.phone
   469→   469→        AND id <> survivor_id
   470→   470→    );
   471→   471→
   472→   472→    -- Mover cualquier otra referencia (orders, etc.) — usando
   473→   473→    -- bloque anónimo para no fallar si la columna no existe.
   474→   474→    BEGIN
   475→   475→      UPDATE orders
   476→   476→      SET customer_id = survivor_id
   477→   477→      WHERE customer_id IN (
   478→   478→        SELECT id FROM customers
   479→   479→        WHERE organization_id = dup_row.organization_id
   480→   480→          AND phone = dup_row.phone
   481→   481→          AND id <> survivor_id
   482→   482→      );
   483→   483→    EXCEPTION WHEN OTHERS THEN NULL;
   484→   484→    END;
   485→   485→
   486→   486→    -- Borrar los duplicados (todo menos el superviviente)
   487→   487→    DELETE FROM customers
   488→   488→    WHERE organization_id = dup_row.organization_id
   489→   489→      AND phone = dup_row.phone
   490→   490→      AND id <> survivor_id;
   491→   491→  END LOOP;
   492→   492→END $$;
   493→   493→
   494→   494→-- 9b. De-duplicar por (organization_id, email) — mismo patrón
   495→   495→DO $$
   496→   496→DECLARE
   497→   497→  dup_row record;
   498→   498→  survivor_id uuid;
   499→   499→BEGIN
   500→   500→  FOR dup_row IN
   501→   501→    SELECT organization_id, email
   502→   502→    FROM customers
   503→   503→    WHERE email IS NOT NULL
   504→   504→    GROUP BY organization_id, email
   505→   505→    HAVING COUNT(*) > 1
   506→   506→  LOOP
   507→   507→    SELECT id INTO survivor_id
   508→   508→    FROM customers
   509→   509→    WHERE organization_id = dup_row.organization_id
   510→   510→      AND email = dup_row.email
   511→   511→    ORDER BY created_at DESC, updated_at DESC
   512→   512→    LIMIT 1;
   513→   513→
   514→   514→    UPDATE reservations
   515→   515→    SET customer_id = survivor_id
   516→   516→    WHERE customer_id IN (
   517→   517→      SELECT id FROM customers
   518→   518→      WHERE organization_id = dup_row.organization_id
   519→   519→        AND email = dup_row.email
   520→   520→        AND id <> survivor_id
   521→   521→    );
   522→   522→
   523→   523→    BEGIN
   524→   524→      UPDATE orders
   525→   525→      SET customer_id = survivor_id
   526→   526→      WHERE customer_id IN (
   527→   527→        SELECT id FROM customers
   528→   528→        WHERE organization_id = dup_row.organization_id
   529→   529→          AND email = dup_row.email
   530→   530→          AND id <> survivor_id
   531→   531→      );
   532→   532→    EXCEPTION WHEN OTHERS THEN NULL;
   533→   533→    END;
   534→   534→
   535→   535→    DELETE FROM customers
   536→   536→    WHERE organization_id = dup_row.organization_id
   537→   537→      AND email = dup_row.email
   538→   538→      AND id <> survivor_id;
   539→   539→  END LOOP;
   540→   540→END $$;
   541→   541→
   542→   542→-- 9c. Ahora sí, crear los índices UNIQUE (ya no hay duplicados)
   543→   543→CREATE UNIQUE INDEX IF NOT EXISTS customers_org_phone_uniq
   544→   544→  ON customers(organization_id, phone)
   545→   545→  WHERE phone IS NOT NULL;
   546→   546→
   547→   547→CREATE UNIQUE INDEX IF NOT EXISTS customers_org_email_uniq
   548→   548→  ON customers(organization_id, email)
   549→   549→  WHERE email IS NOT NULL;
   550→   550→
   551→   551→-- ============================================================
   552→   552→-- 10. CHECK CONSTRAINTS EN COLUMNAS TIPO ENUM
   553→   553→-- ============================================================
   554→   554→-- Usamos NOT VALID para que el constraint SOLO se aplique a filas
   555→   555→-- nuevas y futuras, no a las existentes. Si ya hay filas con valores
   556→   556→-- inválidos (por seed viejos o bugs previos), el ALTER no fallará.
   557→   557→-- Después, opcionalmente, se puede hacer VALIDATE CONSTRAINT para
   558→   558→-- verificar que todo cumple.
   559→   559→
   560→   560→ALTER TABLE users
   561→   561→  DROP CONSTRAINT IF EXISTS users_role_check;
   562→   562→ALTER TABLE users
   563→   563→  ADD CONSTRAINT users_role_check
   564→   564→  CHECK (role IN ('SUPER_ADMIN','ADMIN','STAFF')) NOT VALID;
   565→   565→
   566→   566→ALTER TABLE orders
   567→   567→  DROP CONSTRAINT IF EXISTS orders_status_check;
   568→   568→ALTER TABLE orders
   569→   569→  ADD CONSTRAINT orders_status_check
   570→   570→  CHECK (status IN ('PENDING','PREPARING','SERVED','COMPLETED','CANCELLED')) NOT VALID;
   571→   571→
   572→   572→ALTER TABLE reservations
   573→   573→  DROP CONSTRAINT IF EXISTS reservations_status_check;
   574→   574→ALTER TABLE reservations
   575→   575→  ADD CONSTRAINT reservations_status_check
   576→   576→  CHECK (status IN ('PENDING','CONFIRMED','SEATED','COMPLETED','CANCELLED','NO_SHOW')) NOT VALID;
   577→   577→
   578→   578→ALTER TABLE tables
   579→   579→  DROP CONSTRAINT IF EXISTS tables_status_check;
   580→   580→ALTER TABLE tables
   581→   581→  ADD CONSTRAINT tables_status_check
   582→   582→  CHECK (status IN ('AVAILABLE','OCCUPIED','RESERVED','PREPARING','OUT_OF_SERVICE')) NOT VALID;
   583→   583→
   584→   584→-- ============================================================
   585→   585→-- 11. POLICIES DELETE FALTANTES
   586→   586→-- ============================================================
   587→   587→-- IMPORTANTE: Las tablas opcionales (notifications, chat_channels,
   588→   588→-- chat_messages, import_jobs) pueden no existir si algunas
   589→   589→-- migraciones intermedias no se ejecutaron. Postgres NO permite
   590→   590→-- DROP POLICY IF EXISTS sobre una tabla que no existe (error 42P01).
   591→   591→-- Por eso envolvemos cada operación en un bloque DO que primero
   592→   592→-- verifica si la tabla existe.
   593→   593→
   594→   594→-- notifications
   595→   595→DO $$
   596→   596→BEGIN
   597→   597→  IF EXISTS (SELECT 1 FROM information_schema.tables
   598→   598→             WHERE table_schema = 'public' AND table_name = 'notifications') THEN
   599→   599→    DROP POLICY IF EXISTS notifications_tenant_delete ON notifications;
   600→   600→    CREATE POLICY notifications_tenant_delete ON notifications
   601→   601→      FOR DELETE USING (organization_id = current_user_org_id() or is_current_user_super_admin());
   602→   602→  END IF;
   603→   603→END $$;
   604→   604→
   605→   605→-- chat_channels
   606→   606→DO $$
   607→   607→BEGIN
   608→   608→  IF EXISTS (SELECT 1 FROM information_schema.tables
   609→   609→             WHERE table_schema = 'public' AND table_name = 'chat_channels') THEN
   610→   610→    DROP POLICY IF EXISTS chat_channels_tenant_delete ON chat_channels;
   611→   611→    CREATE POLICY chat_channels_tenant_delete ON chat_channels
   612→   612→      FOR DELETE USING (organization_id = current_user_org_id() or is_current_user_super_admin());
   613→   613→  END IF;
   614→   614→END $$;
   615→   615→
   616→   616→-- chat_messages
   617→   617→DO $$
   618→   618→BEGIN
   619→   619→  IF EXISTS (SELECT 1 FROM information_schema.tables
   620→   620→             WHERE table_schema = 'public' AND table_name = 'chat_messages') THEN
   621→   621→    DROP POLICY IF EXISTS chat_messages_tenant_delete ON chat_messages;
   622→   622→    CREATE POLICY chat_messages_tenant_delete ON chat_messages
   623→   623→      FOR DELETE USING (organization_id = current_user_org_id() or is_current_user_super_admin());
   624→   624→  END IF;
   625→   625→END $$;
   626→   626→
   627→   627→-- import_jobs
   628→   628→DO $$
   629→   629→BEGIN
   630→   630→  IF EXISTS (SELECT 1 FROM information_schema.tables
   631→   631→             WHERE table_schema = 'public' AND table_name = 'import_jobs') THEN
   632→   632→    DROP POLICY IF EXISTS import_jobs_tenant_delete ON import_jobs;
   633→   633→    CREATE POLICY import_jobs_tenant_delete ON import_jobs
   634→   634→      FOR DELETE USING (organization_id = current_user_org_id() or is_current_user_super_admin());
   635→   635→  END IF;
   636→   636→END $$;
   637→   637→
   638→   638→-- ============================================================
   639→   639→-- 12. COMMENTS
   640→   640→-- ============================================================
   641→   641→COMMENT ON TABLE table_groups IS 'Groups of tables for zone management (e.g., Terrace A, VIP, Bar). Each group belongs to one organization.';
   642→   642→COMMENT ON FUNCTION transfer_reservation IS 'Atomic transfer of a reservation between tables. Validates org ownership and old table id.';
   643→   643→COMMENT ON FUNCTION update_customer_metrics IS 'Updates customer visit counters. Increments on COMPLETED, decrements when reverting from COMPLETED.';
   644→   644→
   645→   645→-- ============================================================
   646→   646→-- FIN — Verifica con:
   647→   647→--   SELECT * FROM pg_policies WHERE tablename = 'users' ORDER BY policyname;
   648→   648→--   SELECT * FROM pg_indexes WHERE schemaname = 'public' AND indexname LIKE '%_uniq';
   649→   649→-- ============================================================
   650→   650→